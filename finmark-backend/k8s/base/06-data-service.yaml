apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-service
  namespace: finmark
  labels:
    app: data-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: data-service
  template:
    metadata:
      labels:
        app: data-service
        tier: backend
    spec:
      containers:
        - name: data-service
          image: finmark/data-service:latest
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              value: "3003"
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: finmark-config
                  key: DATABASE_URL
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: finmark
spec:
  selector:
    app: data-service
  ports:
    - port: 3003
      targetPort: 3003
